<UserControl x:Class="BiliLiveDanmaku.Modules.ModuleBorder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BiliLiveDanmaku.Modules"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderThickness="0,1,0,0" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Button x:Name="RemoveBtn" Content="🗙" HorizontalAlignment="Right" Margin="2" Grid.Row="0" VerticalAlignment="Top" Width="22" Click="RemoveBtn_Click"/>
            <Border x:Name="ContentArea" Grid.Row="1">
                
            </Border>
        </Grid>
    </Border>
</UserControl>
